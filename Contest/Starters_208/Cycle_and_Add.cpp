//https://www.codechef.com/problems/CYCADEQ

#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define nl "\n"
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int tc;
    cin >> tc;
    while (tc--)
    {
        int n, c;
        cin >> n >> c;
        vector<int> b(n);
        vector<int> d(n);
        for (int i = 0; i < n; i++)
            cin >> b[i];
        for (int i = 0; i < n; i++)
            cin >> d[i];

        
    }
    return 0;
}